import React from "react";

function NavBar() {
  // update the JSX being returned!
  return <nav>
    NavBar
    <div>
      <a href="#home">Home</a>
      <a href="#about">About</a>
      </div>
  </nav>;
}

export default NavBar;
